# Copyright Disclaimer: AI-Generated Content
# This file was created by GitHub Copilot, an AI coding assistant.
# AI-generated content is not subject to copyright protection and is provided
# without any warranty, express or implied, including warranties of merchantability,
# fitness for a particular purpose, or non-infringement.
# Use at your own risk.

name: Build APK

on:
  push:
    branches: [ main, develop, copilot/** ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      actions: read
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Install NDK
      run: |
        echo "y" | sdkmanager "ndk;21.4.7075529"
        
    - name: Setup Rust toolchain
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: aarch64-linux-android
        
    - name: Install cargo-xdk
      run: |
        cargo install cargo-xdk --git https://github.com/tiann/cargo-xdk
        
    - name: Download rootfs for testing (if not exists)
      run: |
        if [ ! -f app/src/main/assets/rootfs.7z ]; then
          mkdir -p app/src/main/assets
          # Download the test rootfs from the project's own release
          # Note: In production, rootfs should be:
          # 1. Bundled with the app in the repository, OR
          # 2. Verified with SHA256 checksum, OR  
          # 3. Signed and verified
          # This download is for CI testing purposes only and uses the project's own release
          curl -L -o /tmp/rootfs.7z https://github.com/cyanmint/twoyi/releases/download/original/rootfs.7z
          # TODO: Add checksum verification when stable release is available
          # echo "EXPECTED_SHA256 /tmp/rootfs.7z" | sha256sum -c -
          mv /tmp/rootfs.7z app/src/main/assets/rootfs.7z
        fi
        
    - name: Download rom.ini for testing (if not exists)
      run: |
        if [ ! -f app/src/main/assets/rom.ini ]; then
          mkdir -p app/src/main/assets
          # Create a basic rom.ini file
          cat > app/src/main/assets/rom.ini << EOF
        author=weishu
        version=1.0.0
        code=1
        desc=Official TwoYi ROM
        md5=
        EOF
        fi
        
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build with Gradle
      run: ./gradlew assembleRelease --stacktrace
      
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: twoyi-apk
        path: app/build/outputs/apk/release/*.apk
        retention-days: 30
        
    - name: Upload build reports on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: build-reports
        path: app/build/reports/
        retention-days: 7
