# This file was generated by AI (GitHub Copilot) and is NOT APPLICABLE for copyright.
# This is part of the threetwi project, a fork of twoyi (https://github.com/twoyi/twoyi).
#
# This project builds as BOTH:
# - A cdylib (JNI library) for Android app use: libtwoyi.so
# - A standalone binary executable: twoyi-server

[package]
name = "twoyi"
version = "0.1.0"
edition = "2021"

# Build as both a JNI library AND a standalone binary
[lib]
name = "twoyi"
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[[bin]]
name = "twoyi-server"
path = "src/main.rs"

[features]
default = []
# Enable legacy mode with OpenGL renderer support
legacy = []

[dependencies]
libc = "0.2.112"
once_cell = "1.9.0"
log = "0.4.14"
env_logger = "0.10"
uinput-sys = "0.1.7"
clap = { version = "4.4", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
unix_socket = "0.5.0"

# Android-specific dependencies (for JNI) - only on Android
[target.'cfg(target_os = "android")'.dependencies]
android_logger = "0.10"
ndk = "0.6.0"
ndk-sys = "0.3.0"
jni = { version = "0.19.0", default-features = false }

[patch.crates-io]
uinput-sys = { git = 'https://github.com/tiann/rust-uinput-sys' }

[profile.release]
opt-level = "s"
lto = true
strip = true
